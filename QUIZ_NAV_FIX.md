# Quiz Navigation Fix

Updated teacher navigation to avoid duplicate quiz links.

- Teacher sees only **Quiz Studio** for quiz creation + published quiz management.
- Student sees only **Quiz** for attempting published quizzes.
- Quiz Reports are accessed through **Analytics**, not as a separate top-level navbar item.
